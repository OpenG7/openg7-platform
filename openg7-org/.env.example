# Front Angular — configuration runtime (SSR + navigateur)
# Copiez en `.env` ou chargez ces variables via votre shell/direnv.

# Connexion au CMS
API_URL=http://localhost:1337
API_WITH_CREDENTIALS=true
API_TOKEN=og7_frontend_readonly_token
HOMEPAGE_PREVIEW_TOKEN=preview-token

# Internationalisation
I18N_PREFIX=/assets/i18n/

# Authentification & fonctionnalités
AUTH_MODE=hybrid
FEATURE_FLAGS={}

# Intégrations optionnelles
NOTIFICATION_WEBHOOK_URL=
ANALYTICS_ENDPOINT=

# Serveur SSR (utilisé par `npm run serve:ssr` ou l'hébergement Node)
PORT=4000
